# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
venv
.venv/
.pytest_cache/

# Node / frontend build (issue #796) — node_modules is never committed.
# NOTE: apps/dashboard/static/dist/ IS committed on purpose: the dashboard is
# served from disk with no build step, so the emitted bundle must ship in git.
node_modules/

# Environment variables
.env
.env.*
!.env.example

# Claude Code — local/generated files
.claude/settings.local.json
**/.claude/settings.local.json
.claude/worktrees/
.claude/context-digest.md

# Generated code-navigation snapshot
codedb.snapshot

# Runtime
*.pid
*.log
*.db
*.bak
:memory:
logs/
sprints/

# Generated / runtime artifacts (issue #762 secrets + hygiene audit)
STATUS.md
requirements.txt.bak
.commander/estimates/
.commander/reports/

# Sprint manager user-specific config (template lives at .commander/sprint.yaml.example)
.commander/sprint.yaml
.commander/runtime/
.commander/logs/
.commander/sprints/
.commander/alerts/
.commander/bulk-jobs/
.commander/docs/

# dashboard temp
apps/dashboard/projects.json
apps/dashboard/tests/

# Backup config — stores gist ID, gitignored by design
.commander/backup_config.json

# Migration artifacts — local data state, never committed
.commander/project_todos_store.json.migrated

# Runtime state — attachment bare-clone cache and draft uploads (issue #188)
apps/dashboard/runtime/
# Added by code-review-graph
.code-review-graph/











# Local settings fallback store — per-clone artifact, never commit (issue #1359)
.commander/settings_store.json

