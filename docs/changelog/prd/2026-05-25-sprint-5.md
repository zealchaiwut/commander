# PRD Changelog — Sprint 5 (2026-05-25)

**Sprint(s) included:** sprint-5 and earlier develop work
**Date merged to master:** 2026-05-25
**PR:** #58 — [develop → master](https://github.com/zealchaiwut/commander/pull/58)
**Environment:** PRD (port 8000, `master` branch)

---

## What Changed

This merge promoted the nested project layout, multi-repo sprint manager
support, and a set of path-hardcoding fixes that make Commander relocatable
(works from any clone path, not just `~/commander`).

### Features & Fixes

| Issue | Title | Type |
|---|---|---|
| #57 | Fix: remove hardcoded `~/commander` paths — use self-locating hook wrappers | fix |
| #52 | Restructure repo: separate dashboard app from project root | chore |
| #61 | Restructure project folders to nested layout | chore |
| #49 | Sprint manager: multi-repo support | feature |
| #47 | Sprint manager: use sprint branch as COMMANDER_MERGE_TARGET | feature |
| #27 | Separate PRD/UAT databases with env isolation | feature |

---

## What to Test on PRD

Restart PRD after pulling master: `restart-prd`

### Checklist

- [ ] **Relocatable hooks (#57)**
  - Where to look: trigger a Claude Code tool call in any project's coder/tester clone
  - Verify: the event appears in the PRD dashboard at `http://localhost:8000` — no `FileNotFoundError` in the server logs

- [ ] **Multi-repo sprint manager (#49)**
  - Where to look: Sprint Mgmt tab → project selector
  - Verify: you can select different repos and run sprints for each independently

- [ ] **PRD/UAT database isolation (#27)**
  - Where to look: `cmdr-status`
  - Verify: PRD uses `commander.db`, UAT uses `commander-uat.db`; events posted to port 8000 do not appear in the UAT dashboard and vice versa

- [ ] **Regression check**
  - Dashboard loads at `http://localhost:8000` without errors
  - Project cards appear for all tracked repos
  - Agent events are received and shown in the Agents tab
  - Sprint Mgmt tab opens correctly

---

## Rollback

```bash
git checkout master
git revert HEAD --no-edit
git push origin master
restart-prd
```

---

## Notes

- This was the first merge after the nested layout restructure. If any path issues surface, check `.commander/sprint.yaml` — paths must point to the new clone locations (`~/dev/<project>/coder`, `~/dev/<project>/tester`).
- The next PRD merge should include sprint 6 and 7 work (sprint management panel, inline run/kill, rerun button, Bangkok timezone fix).
