[pytest]
# Register custom marks so `-m selenium` / `-m smoke` do not warn (issue #334).
markers =
    selenium: real browser tests requiring Chrome and a running dashboard at TEST_BASE_URL
    smoke: fast tests run on every PR in the pytest-smoke CI workflow

testpaths = tests
