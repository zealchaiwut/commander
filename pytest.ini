[pytest]
# Register custom marks so `-m agent_browser` / `-m smoke` do not warn.
markers =
    agent_browser: real browser tests via the agent-browser CLI (opt-in; requires `agent-browser install`)
    smoke: fast tests run on every PR in the pytest-smoke CI workflow

testpaths = tests
